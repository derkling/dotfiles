#!/bin/bash

# Check speaker mute status
amixer -c 0 get Speaker | grep off >/dev/null
if [ $? -eq 1 ]; then
	CMD=mute
else
	CMD=unmute
fi

amixer -c 0 set Speaker $CMD
amixer -c 0 set Master $CMD

# Update i3status immediately
killall -USR1 i3status
